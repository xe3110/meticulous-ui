import { Meta, Primary, Controls, Story } from '@storybook/addon-docs/blocks';

import * as IconStories from '../Icons.stories';

<Meta of={IconStories} />

# Icons

An Icon can be imported as `meticulous-ui/component/Icons/Add` & can be from the following:

<Primary />
