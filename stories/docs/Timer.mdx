import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';

import * as TimerStories from '../Timer.stories';

<Meta title='components/Timer' of={TimerStories} />

# Timer

A Timer is used display input boxes to enter otp, can be imported as `meticulous-ui/components/Timer`

<Canvas>
  <Story name='Timer'>
    <TimerStories />
  </Story>
</Canvas>
