import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';

import * as LoaderStories from '../Loader.stories';

<Meta title='Atoms/Loader' of={LoaderStories} />

# Loader

A Loader displays five dots (or three in mini mode) in a sequential animation to communicate a loading state. Can be imported as `meticulous-ui/components/Loader`.

Supports two animation styles — **pulse** (default) and **bounce** — along with multiple sizes and color themes.

## Default

<Canvas of={LoaderStories.Default} />

<Controls of={LoaderStories.Default} />

## Sizes

All three sizes with the default pulse animation.

<Canvas of={LoaderStories.Sizes} />

## Themes

<Canvas of={LoaderStories.Themes} />

## Bounce Animation

Dots bounce vertically and scale up at the peak. Enable via the `isBounce` prop.

<Canvas of={LoaderStories.BounceAnimation} />

## Bounce — Different Themes

<Canvas of={LoaderStories.BounceThemes} />

## Mini (3 dots)

Renders only 3 dots with evenly spaced delays. Enable via the `isMini` prop.

<Canvas of={LoaderStories.Mini} />

## Mini — Different Themes

<Canvas of={LoaderStories.MiniThemes} />

## Mini + Bounce

<Canvas of={LoaderStories.MiniBounce} />

## Mini + Bounce — Different Themes

<Canvas of={LoaderStories.MiniBounceThemes} />

## MiniDark (3 dots)

Renders only 3 dots with evenly spaced delays. Enable via the `isMiniDark` prop.

<Canvas of={LoaderStories.MiniDark} />

## MiniDark — Different Themes

<Canvas of={LoaderStories.MiniDarkThemes} />

## MiniDark + Bounce

<Canvas of={LoaderStories.MiniDarkBounce} />

## MiniDark + Bounce — Different Themes

<Canvas of={LoaderStories.MiniDarkBounceThemes} />

## Props

<table>
  <thead>
    <tr>
      <th>Prop</th>
      <th>Type</th>
      <th>Default</th>
      <th>Description</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>
        <code>size</code>
      </td>
      <td>
        <code>'small' | 'medium' | 'large'</code>
      </td>
      <td>
        <code>'medium'</code>
      </td>
      <td>Controls the dot size</td>
    </tr>
    <tr>
      <td>
        <code>theme</code>
      </td>
      <td>
        <code>string</code>
      </td>
      <td>
        <code>'blue'</code>
      </td>
      <td>Color theme of the dots</td>
    </tr>
    <tr>
      <td>
        <code>isBounce</code>
      </td>
      <td>
        <code>boolean</code>
      </td>
      <td>
        <code>false</code>
      </td>
      <td>Switches to the bounce animation; defaults to pulse</td>
    </tr>
    <tr>
      <td>
        <code>isMini</code>
      </td>
      <td>
        <code>boolean</code>
      </td>
      <td>
        <code>false</code>
      </td>
      <td>Renders 3 dots instead of 5</td>
    </tr>
    <tr>
      <td>
        <code>isMiniDark</code>
      </td>
      <td>
        <code>boolean</code>
      </td>
      <td>
        <code>false</code>
      </td>
      <td>Renders 3 dark dots instead of 5</td>
    </tr>
  </tbody>
</table>

## Accessibility

- The wrapper has `role="status"` — an implicit `aria-live="polite"` live region
- A visually-hidden `"Loading"` text node is announced by screen readers when the component mounts
- Each decorative dot has `aria-hidden="true"` to keep the accessibility tree clean
- Animations are suppressed for users with `prefers-reduced-motion: reduce`
